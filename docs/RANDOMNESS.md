# Plan: Write docs/RANDOMNESS.md

## Context

The 100 Days RPG currently randomizes events turn-by-turn (via `sampleEventsForTurn`) and uses a seeded Mulberry32 RNG that's already threaded through `TurnEngine`. However, each playthrough follows the same structural skeleton: the same NPC encounter slots, the same merchant locations, the same location flavour text rotation. This document design formalises a set of complementary systems that use the **run seed** to differentiate playthroughs at a structural level — not just in moment-to-moment dice rolls.

The deliverable is `docs/RANDOMNESS.md`, a design document (no implementation yet). It should be precise enough for a developer to implement any feature from it without needing a follow-up conversation.

---

## What to write

### Foundations to acknowledge (already in the code)

Before designing new features the doc should orient readers on what's already in place:

| Thing | Where |
|---|---|
| Mulberry32 seeded RNG | `src/engine/Random.ts` — `nextMulberry32(state)` |
| `seed` + `rngState` on `GameState` | `src/engine/types.ts:420-422` |
| `rngState` threaded through TurnEngine | `TurnEngine` constructor accepts `randomOverride?` |
| `sampleEventsForTurn(state, rng)` | injectable rng parameter already present |
| `firedEventIds: Set<string>` | one-shot event deduplication |
| `randomTexts: string[]` on `Location` | per-location flavour pool, already defined |
| `eventPool?: string[]` on `Location` | per-location event override already in schema |

The RNG helper to use throughout: call `nextMulberry32(state.rngState)` to get `{value, state}`, then save the new state back. For run-layout decisions (NPC slots, shortcuts, merchant towns) use a **separate deterministic pass over the seed at run-start**, not the live `rngState`, so layout is stable regardless of how many rolls combat/events consume.

---

### Feature 1 — Day-Dependent Location Descriptions

**Problem:** Locations have `randomTexts: string[]` but nothing selects from them.

**Design:**
- Add a pure helper `pickLocationText(location: Location, dayNumber: number, seed: number): string` in `src/engine/GameState.ts`.
- Selection: `index = floor(mulberry32(seed XOR locationId XOR dayNumber).value * randomTexts.length)`. This is deterministic per (run, location, day) tuple without consuming live rngState.
- RoadScreen calls this when rendering the location description, replacing any static `locationText` display.
- No GameState change needed; no migration needed.

---

### Feature 2 — NPC Encounter Slots

**Problem:** Companion-meet and dialogue events fire probabilistically but have no structural placement; a player can go an entire run without meeting key NPCs.

**Design:**
Introduce `RunLayout` — a plain object computed once from the seed and stored on `GameState`:

```ts
interface RunLayout {
  npcSlots: Array<{ locationId: number; npcEventId: string }>;
  merchantTowns: number[];           // subset of town locationIds
  activeShortcuts: Array<{ from: number; to: number; label: string }>;
  eliteSpawns: Array<{ locationId: number; enemyType: string }>;
}
```

`RunLayout` is generated by `generateRunLayout(seed: number): RunLayout` in a new file `src/engine/RunLayout.ts`.

For NPC slots: pre-define 15–20 candidate `{locationRange, npcEventId}` pairs in `RunLayout.ts`. At run-start, the generator selects 5–7 of them deterministically (Fisher-Yates with seed-derived RNG), records the chosen `locationId` within each range. TurnEngine checks `state.runLayout.npcSlots` in `SamplingEvents` phase — if the player is at a slot location and the event hasn't fired, force-queue it (same pattern as boss events).

---

### Feature 3 — Traveling Merchant

**Problem:** Shops are fixed to `hasShop: true` locations. A merchant that appears at different town locations each run creates discovery incentive.

**Design:**
`generateRunLayout` picks 3–4 `isTown` locations (excluding permanent shop towns) as `merchantTowns`. TurnEngine (or EventSystem) treats arrival at a `merchantTowns` location identically to a `hasShop` location for that run. No new event type needed — wire it in the existing Trade action path in TurnEngine, gated by `state.runLayout.merchantTowns.includes(state.currentLocationId)`.

---

### Feature 4 — Roaming Elite Mobs

**Problem:** Combat events are purely probabilistic; there's no sense of a persistent threat occupying a stretch of road.

**Design:**
`generateRunLayout` picks 6–8 `{locationId, enemyType}` elite spawns from wilderness/dungeon locations (locs 15–110). Each is a one-shot combat event: when the player enters that location, if it's not in `clearedCombatLocations`, force-queue a `bandit_ambush`-style event tagged `'location_ambush'`. TurnEngine already handles `location_ambush` tag cleanup (see `TurnEngine.ts:128-131`). No new event definition needed; reuse the `combat_handler`.

The `enemyType` field guides which CombatEngine enemy to instantiate (13 types already in `CombatEngine.ts`).

---

### Feature 5 — Path Shortcuts

**Problem:** Every run traverses the same 125-location linear road. Shortcuts give faster players an occasional structural reward.

**Design:**
Pre-define 12 candidate shortcut pairs `{from, to, label}` in `RunLayout.ts` (e.g. `{from: 28, to: 34, label: "The old smuggler's pass"}`). `generateRunLayout` activates 3 of them. Shortcuts appear as an additional Move option on `RoadScreen` when the player is at a `from` location. Selecting one moves the player directly to `to` and costs 1 extra food (forced march equivalent).

RoadScreen reads `state.runLayout.activeShortcuts.filter(s => s.from === state.currentLocationId)` and renders shortcut buttons. The Move action in TurnEngine needs a `shortcutTo?: number` param variant.

---

### Data Model Changes

Add `runLayout: RunLayout` to `GameState`. Because `RunLayout` is fully re-derivable from `seed`, it can be omitted from `SerializedGameState` and reconstructed on load — or serialized as a plain object (simpler). Recommend serializing it to avoid re-running generation on every load.

`SerializedGameState` already `Omit`s the Set fields; `RunLayout` (all arrays/objects) serializes cleanly as-is.

Schema version: bump `SCHEMA_VERSION` from 6 → 7 in `src/engine/GameState.ts`. Add a migration case in `SaveEngine.migrate()` that generates `runLayout` from the loaded `seed` for v6 saves.

---

## Document structure for RANDOMNESS.md

```
# Randomness & Per-Playthrough Variation

## 1  Overview
## 2  Foundations Already In Place
## 3  Core Architecture: RunLayout
     (diagram of seed → generateRunLayout → GameState.runLayout → features)
## 4  Feature 1: Day-Dependent Descriptions
## 5  Feature 2: NPC Encounter Slots
## 6  Feature 3: Traveling Merchant
## 7  Feature 4: Roaming Elite Mobs
## 8  Feature 5: Path Shortcuts
## 9  Bonus Ideas (4 lightweight extras)
## 10 Data Model
## 11 Files to Create / Modify
## 12 Schema Migration (v6 → v7)
## 13 Implementation Sequence
## 14 Verification
```

The diagram in §3 should be an ASCII/mermaid block:

```
seed (GameState.seed)
  └─ generateRunLayout(seed)          [src/engine/RunLayout.ts]
        ├─ npcSlots[]
        ├─ merchantTowns[]
        ├─ activeShortcuts[]
        └─ eliteSpawns[]
              │
              ▼
        GameState.runLayout            [persisted in SaveFile]
              │
    ┌─────────┴──────────────────────┐
    │         │          │           │
TurnEngine  RoadScreen  EventSystem  ItemSystem
(force-queue (shortcut  (NPC slots)  (merchant
 elites/NPCs) buttons)               trade)
```

---

## Files to create / modify

| File | Change |
|---|---|
| `src/engine/RunLayout.ts` | **Create**: `RunLayout` type + `generateRunLayout(seed)` |
| `src/engine/types.ts` | Add `RunLayout` interface; add `runLayout: RunLayout` to `GameState` and `SerializedGameState` |
| `src/engine/GameState.ts` | Bump `SCHEMA_VERSION` to 7; add `pickLocationText()` helper; initialise `runLayout` in `createNewGameState()` |
| `src/engine/SaveEngine.ts` | Add v6→v7 migration: call `generateRunLayout(state.seed)` |
| `src/engine/TurnEngine.ts` | Force-queue elite spawns + NPC slots; add `shortcutTo` move variant |
| `src/screens/RoadScreen.tsx` | Render shortcut buttons; call `pickLocationText()` for flavour |

---

## Verification

1. Start a new game twice with different seeds — confirm `runLayout` differs between runs.
2. Travel to a location in `npcSlots` — confirm the NPC dialogue event fires.
3. Travel to a `merchantTowns` location without `hasShop` — confirm trade is available.
4. Travel to an elite spawn location — confirm combat triggers and clears on victory.
5. Stand at a shortcut `from` location — confirm button renders; travel via shortcut lands at `to`.
6. Save mid-run, reload — confirm `runLayout` round-trips correctly and shortcuts/NPCs remain consistent.
7. Load a v6 save — confirm migration generates `runLayout` without error.
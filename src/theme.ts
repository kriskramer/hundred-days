// eslint-disable-next-line @typescript-eslint/no-var-requires
const tokens = require('./tokens');

export const Colors = tokens as {
  ink: string;
  inkLight: string;
  parchment: string;
  parchDark: string;
  parchDeep: string;
  blood: string;
  gold: string;
  goldLight: string;
  mist: string;
  green: string;
  greenLight: string;
};
